{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "backend",
      "runtimeExecutable": "powershell.exe",
      "runtimeArgs": ["-ExecutionPolicy", "Bypass", "-File", "start-backend.ps1"],
      "port": 5080
    },
    {
      "name": "frontend",
      "runtimeExecutable": "powershell.exe",
      "runtimeArgs": [
        "-ExecutionPolicy", "Bypass", "-NoProfile", "-Command",
        "Set-Location 'frontend/cashapp-web'; if (-not (Test-Path .env)) { Copy-Item .env.example .env }; if (-not (Test-Path node_modules)) { npm install --no-audit --no-fund }; npm run dev -- --port 3000"
      ],
      "port": 3000
    }
  ]
}
